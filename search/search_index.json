{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"PPLP","text":"<p>Privacy-Preserving Link Prediction \u2014 a Python library enabling two-party Common Neighbors link prediction over distributed graphs without revealing private graph structure.</p> <p>Based on Demirag/Ayday et al. 2022.</p>"},{"location":"#what-it-does","title":"What it does","text":"<p>Two parties (each holding a private graph on separate machines) can compute how many common neighbors a pair of nodes has across their combined graph \u2014 without either party revealing their edges to the other. This is useful for:</p> <ul> <li>Social networks \u2014 friend recommendations across platforms</li> <li>E-commerce \u2014 collaborative product recommendations</li> <li>Telecommunications \u2014 targeted advertising across carriers</li> <li>Bioinformatics \u2014 cross-institutional disease/gene association</li> </ul>"},{"location":"#quick-start","title":"Quick start","text":"<p>Party 2 \u2014 start the server:</p> <pre><code>pip install pplp\npplp-server party2_graph.csv --host 0.0.0.0 --port 8000\n</code></pre> <p>Party 1 \u2014 run the query:</p> <pre><code>pip install pplp\n</code></pre> <pre><code>from pplp import Graph, compute_cn_remote, party2_client\n\ngraph1 = Graph.from_edge_list([(\"Alice\", \"Bob\"), (\"Alice\", \"Charlie\")])\n\nwith party2_client(\"http://&lt;party2-ip&gt;:8000\") as client:\n    cn = compute_cn_remote(graph1, client, \"Alice\", \"Dave\")\n    print(f\"Common Neighbors: {cn}\")\n</code></pre> <p>See the Usage Guide for full details.</p>"},{"location":"api/","title":"API Reference","text":""},{"location":"api/#compute_cn_remote","title":"<code>compute_cn_remote</code>","text":"<pre><code>from pplp import compute_cn_remote\n</code></pre>"},{"location":"api/#compute_cn_remotegraph1-party2_client-x-y","title":"<code>compute_cn_remote(graph1, party2_client, x, y)</code>","text":"<p>Compute the Common Neighbors count for the candidate pair <code>(x, y)</code> across both parties' graphs using the Ayday et al. privacy-preserving protocol over HTTP.</p> <p>Party 1 holds <code>graph1</code> locally and connects to Party 2's server. Only Party 1 learns the result.</p> <p>Parameters:</p> <ul> <li><code>graph1</code> \u2014 <code>Graph</code> \u2014 Party 1's private graph</li> <li><code>party2_client</code> \u2014 HTTP client pointed at Party 2's server (use <code>party2_client()</code>)</li> <li><code>x</code> \u2014 <code>str</code> \u2014 first node in the candidate pair</li> <li><code>y</code> \u2014 <code>str</code> \u2014 second node in the candidate pair</li> </ul> <p>Returns: <code>int</code> \u2014 Common Neighbors count on the joint graph</p> <p>Raises:</p> <ul> <li><code>ValueError</code> \u2014 if <code>x</code> and <code>y</code> are direct neighbors in <code>graph1</code></li> <li><code>DirectLinkFound</code> \u2014 if <code>x</code> and <code>y</code> are direct neighbors in <code>graph2</code> (reported by Party 2)</li> </ul>"},{"location":"api/#party2_client","title":"<code>party2_client</code>","text":"<pre><code>from pplp import party2_client\n</code></pre>"},{"location":"api/#party2_clientbase_url-timeout300","title":"<code>party2_client(base_url, timeout=30.0)</code>","text":"<p>Context manager yielding an HTTP client configured for Party 2's server.</p> <p>Parameters:</p> <ul> <li><code>base_url</code> \u2014 <code>str</code> \u2014 Party 2's server URL, e.g. <code>\"http://192.168.1.10:8000\"</code></li> <li><code>timeout</code> \u2014 <code>float</code> \u2014 request timeout in seconds (default <code>30.0</code>)</li> </ul> <p>Example:</p> <pre><code>with party2_client(\"http://192.168.1.10:8000\") as client:\n    cn = compute_cn_remote(graph1, client, \"A\", \"E\")\n</code></pre>"},{"location":"api/#graph","title":"<code>Graph</code>","text":"<pre><code>from pplp import Graph\n</code></pre> <p>A simple undirected graph backed by an adjacency list. Node IDs are strings.</p>"},{"location":"api/#graph_1","title":"<code>Graph()</code>","text":"<p>Create an empty graph.</p>"},{"location":"api/#graphfrom_edge_listedges","title":"<code>Graph.from_edge_list(edges)</code>","text":"<p>Create a graph from a list of <code>(node_a, node_b)</code> tuples.</p> <pre><code>g = Graph.from_edge_list([(\"A\", \"B\"), (\"B\", \"C\")])\n</code></pre> <p>Parameters:</p> <ul> <li><code>edges</code> \u2014 <code>list[tuple[str, str]]</code></li> </ul> <p>Returns: <code>Graph</code></p>"},{"location":"api/#graphadd_edgeu-v","title":"<code>graph.add_edge(u, v)</code>","text":"<p>Add an undirected edge. Creates nodes if they don't exist. Duplicate edges are silently ignored.</p>"},{"location":"api/#graphneighborsnode","title":"<code>graph.neighbors(node)</code>","text":"<p>Return the neighbor set of a node. Returns an empty set if the node doesn't exist.</p> <p>Returns: <code>set[str]</code></p>"},{"location":"api/#graphhas_edgeu-v","title":"<code>graph.has_edge(u, v)</code>","text":"<p>Check whether an edge exists between <code>u</code> and <code>v</code>.</p> <p>Returns: <code>bool</code></p>"},{"location":"api/#graphlocal_intersectionx-y","title":"<code>graph.local_intersection(x, y)</code>","text":"<p>Return the set of common neighbors of <code>x</code> and <code>y</code> within this graph.</p> <p>Returns: <code>set[str]</code></p>"},{"location":"api/#directlinkfound","title":"<code>DirectLinkFound</code>","text":"<pre><code>from pplp import DirectLinkFound\n</code></pre> <p>Raised when the candidate pair <code>(x, y)</code> are direct neighbors in Party 2's graph. Per the paper (Section 2.2), this is stronger evidence for link prediction than a Common Neighbors score.</p> <p>Inherits from <code>Exception</code>.</p>"},{"location":"api/#compute_cn-local-simulation","title":"<code>compute_cn</code> (local simulation)","text":"<pre><code>from pplp import compute_cn\n</code></pre>"},{"location":"api/#compute_cngraph1-graph2-x-y","title":"<code>compute_cn(graph1, graph2, x, y)</code>","text":"<p>Local version of the protocol \u2014 both graphs in the same process. Useful for testing and development; not for real two-party use.</p> <p>Parameters: same as <code>compute_cn_remote</code> but takes a <code>Graph</code> instead of an HTTP client for <code>graph2</code>.</p> <p>Returns: <code>int</code></p> <p>Raises: <code>ValueError</code>, <code>DirectLinkFound</code> (same conditions as <code>compute_cn_remote</code>)</p>"},{"location":"api/#pplp-server-cli","title":"<code>pplp-server</code> CLI","text":"<pre><code>pplp-server &lt;graph_file&gt; [--host HOST] [--port PORT]\n</code></pre> <p>Starts Party 2's FastAPI server. <code>graph_file</code> is a two-column CSV (no header) of edges:</p> <pre><code>Alice,Bob\nBob,Charlie\n</code></pre> Argument Default Description <code>graph_file</code> required Path to edge-list CSV <code>--host</code> <code>0.0.0.0</code> Bind address <code>--port</code> <code>8000</code> Port"},{"location":"protocol/","title":"Protocol Details","text":"<p>This page explains the cryptographic protocol implemented by PPLP, based on Demirag/Ayday et al. 2022.</p>"},{"location":"protocol/#problem","title":"Problem","text":"<p>Two parties (Graph 1 and Graph 2) each hold a graph over partially overlapping node sets. Graph 1 wants to know: how many common neighbors do nodes x and y have in the combined graph? \u2014 without either party revealing their edges.</p>"},{"location":"protocol/#threat-model","title":"Threat model","text":"<ul> <li>Semi-honest (honest-but-curious): Both parties follow the protocol correctly but try to infer information from messages they receive.</li> <li>Only Graph 1 learns the result. Graph 2 learns nothing beyond the common public inputs (node identifiers x and y).</li> <li>Node identifiers match across graphs (e.g., both parties use email addresses).</li> <li>x and y are not already direct neighbors in either graph.</li> </ul>"},{"location":"protocol/#what-leaks","title":"What leaks","text":"<p>The basic protocol leaks intermediate intersection sizes (local2, crossover1, crossover2, overlap) to Graph 1. The paper shows this leakage is minimal for realistic graph sizes \u2014 see Section 3.3 of the paper for the full analysis.</p> <p>A heavier variant using additively homomorphic encryption eliminates this leakage entirely (Section 4.1), but is not implemented here.</p>"},{"location":"protocol/#the-formula","title":"The formula","text":"<p>Common Neighbors on the joint graph is decomposed as:</p> <pre><code>CN = local1 + local2 + crossover1 + crossover2 - overlap\n</code></pre> <p>Where:</p> Term Definition How computed <code>local1</code> |\u0393\u2081(x) \u2229 \u0393\u2081(y)| Graph 1 computes locally <code>local2</code> |\u0393\u2082(x) \u2229 \u0393\u2082(y)| Graph 2 computes locally <code>crossover1</code> |(\u0393\u2081(x) \\ local1) \u2229 (\u0393\u2082(y) \\ local2)| PSI call #1 <code>crossover2</code> |(\u0393\u2081(y) \\ local1) \u2229 (\u0393\u2082(x) \\ local2)| PSI call #2 <code>overlap</code> |local1_set \u2229 local2_set| PSI call #3 <p>The subtraction of <code>overlap</code> prevents double-counting nodes that appear in both local intersections.</p>"},{"location":"protocol/#step-by-step","title":"Step by step","text":""},{"location":"protocol/#1-local-preparation-each-party-independently","title":"1. Local preparation (each party independently)","text":"<p>Each party computes:</p> <ul> <li>Neighbor sets: \u0393\u2081(x), \u0393\u2081(y) for Graph 1; \u0393\u2082(x), \u0393\u2082(y) for Graph 2</li> <li>Local intersection: e.g., <code>local1_set = \u0393\u2081(x) \u2229 \u0393\u2081(y)</code></li> <li>Remove local intersection members from neighbor sets before PSI</li> </ul> <p>The removal step is critical \u2014 without it, the crossover terms would double-count nodes already captured by the local terms.</p>"},{"location":"protocol/#2-three-psi-cardinality-calls","title":"2. Three PSI-cardinality calls","text":"<p>Each call uses Private Set Intersection in cardinality-only mode: it reveals only the size of the intersection, not which elements are in it.</p> <ol> <li><code>crossover1 = PSI-CA(\u0393\u2081(x) \\ local1, \u0393\u2082(y) \\ local2)</code></li> <li><code>crossover2 = PSI-CA(\u0393\u2081(y) \\ local1, \u0393\u2082(x) \\ local2)</code></li> <li><code>overlap = PSI-CA(local1_set, local2_set)</code></li> </ol>"},{"location":"protocol/#3-combine","title":"3. Combine","text":"<p>Graph 1 computes: <code>CN = local1 + local2 + crossover1 + crossover2 - overlap</code></p>"},{"location":"protocol/#worked-example","title":"Worked example","text":"<p>From the paper (Figure 6), with nodes A and E:</p> <p>Graph 1 (Alice): A\u2014B, A\u2014C, A\u2014G, A\u2014H, E\u2014C, E\u2014D</p> <p>Graph 2 (Bob): A\u2014C, A\u2014F, A\u2014K, E\u2014B, E\u2014C, E\u2014D, E\u2014F</p> Step Computation Result local1 \u0393\u2081(A) \u2229 \u0393\u2081(E) {C} \u2192 size 1 local2 \u0393\u2082(A) \u2229 \u0393\u2082(E) {C, F} \u2192 size 2 Reduced \u0393\u2081(A) {H, G, B, C} \\ {H, G, B} Reduced \u0393\u2081(E) {C, D} \\ {D} Reduced \u0393\u2082(A) {C, F, K} \\ {K} Reduced \u0393\u2082(E) {B, C, D, F} \\ {B, D} crossover1 |{H,G,B} \u2229 {B,D}| 1 crossover2 |{D} \u2229 {K}| 0 overlap |{C} \u2229 {C,F}| 1 CN 1 + 2 + 1 + 0 - 1 3"},{"location":"protocol/#psi-implementation","title":"PSI implementation","text":"<p>We use OpenMined PSI (<code>openmined-psi</code> on PyPI):</p> <ul> <li>ECDH-based protocol</li> <li>Cardinality-only mode (<code>reveal_intersection=False</code>)</li> <li>Golomb Coded Sets (GCS) for compressed representation</li> <li>C++ backend, pip-installable, supports macOS Apple Silicon and Linux</li> </ul>"},{"location":"usage/","title":"Usage Guide","text":""},{"location":"usage/#installation","title":"Installation","text":"<pre><code>pip install pplp\n</code></pre>"},{"location":"usage/#setup","title":"Setup","text":"<p>Each party installs <code>pplp</code> independently on their own machine. Node IDs must match across both graphs (e.g., both use email addresses or phone numbers for shared nodes).</p>"},{"location":"usage/#party-2-start-the-server","title":"Party 2: start the server","text":"<p>Party 2 prepares a CSV edge list (no header, one edge per line) and starts the server:</p> <pre><code>Alice,Bob\nAlice,Charlie\nBob,Dave\n</code></pre>"},{"location":"usage/#option-1-ngrok-tunnel-no-firewall-config","title":"Option 1: ngrok tunnel (no firewall config)","text":"<p>Install the tunnel dependency and use <code>--tunnel</code>:</p> <pre><code>pip install pplp[tunnel]\npplp-server party2_graph.csv --tunnel\n</code></pre> <p>The server prints a public URL (e.g. <code>https://abc123.ngrok.io</code>) \u2014 share this with Party 1. No router config or port forwarding needed.</p>"},{"location":"usage/#option-2-manual-firewall","title":"Option 2: Manual firewall","text":"<p>If you prefer to open ports yourself:</p> <pre><code>pplp-server party2_graph.csv --host 0.0.0.0 --port 8000\n</code></pre> <p>Options:</p> Flag Default Description <code>--host</code> <code>0.0.0.0</code> Bind address <code>--port</code> <code>8000</code> Port <code>--tunnel</code> <code>False</code> Expose via ngrok tunnel"},{"location":"usage/#party-1-run-the-query","title":"Party 1: run the query","text":"<p>Party 1 builds their graph in Python and connects to Party 2's server:</p> <pre><code>from pplp import Graph, compute_cn_remote, party2_client\n\ngraph1 = Graph.from_edge_list([\n    (\"Alice\", \"Bob\"), (\"Alice\", \"Charlie\"), (\"Alice\", \"Grace\"), (\"Alice\", \"Henry\"),\n    (\"Eve\", \"Charlie\"), (\"Eve\", \"Diana\"),\n])\n\nwith party2_client(\"http://&lt;party2-ip&gt;:8000\") as client:  # or https://... if using --tunnel\n    cn = compute_cn_remote(graph1, client, \"Alice\", \"Eve\")\n    print(f\"Common Neighbors: {cn}\")\n</code></pre> <p>Only Party 1 learns the result. Party 2's graph structure is never revealed.</p>"},{"location":"usage/#building-graphs","title":"Building graphs","text":"<pre><code>from pplp import Graph\n\n# From an edge list\ngraph = Graph.from_edge_list([\n    (\"alice\", \"bob\"),\n    (\"alice\", \"charlie\"),\n    (\"bob\", \"dave\"),\n])\n\n# Or incrementally\ngraph = Graph()\ngraph.add_edge(\"alice\", \"bob\")\ngraph.add_edge(\"alice\", \"charlie\")\n</code></pre>"},{"location":"usage/#handling-edge-cases","title":"Handling edge cases","text":""},{"location":"usage/#direct-neighbors","title":"Direct neighbors","text":"<p>If the candidate pair is already directly connected, there's no need for link prediction:</p> <pre><code>try:\n    cn = compute_cn_remote(graph1, client, \"Alice\", \"Eve\")\nexcept ValueError:\n    # Alice and Eve are already direct neighbors in Party 1's graph\n    print(\"Already connected in your graph\")\nexcept DirectLinkFound:\n    # Alice and Eve are direct neighbors in Party 2's graph\n    print(\"Direct link exists in the other party's graph\")\n</code></pre>"},{"location":"usage/#missing-nodes","title":"Missing nodes","text":"<p>If a node doesn't exist in one graph, its neighbor set is treated as empty \u2014 the protocol still works, you just get zero contribution from that side.</p>"},{"location":"usage/#whats-happening-under-the-hood","title":"What's happening under the hood","text":"<p>For a candidate pair (x, y), <code>compute_cn_remote</code> runs the Demirag/Ayday et al. protocol over HTTP:</p> <ol> <li>Party 1 calls <code>/prepare</code> \u2014 Party 2 checks for a direct link and returns its local intersection size</li> <li>Three PSI-cardinality calls (each a pair of HTTP round-trips to <code>/psi/{id}/setup</code> and <code>/psi/{id}/respond</code>) reveal only the sizes of crossover intersections</li> <li>Party 1 combines: <code>CN = local1 + local2 + crossover1 + crossover2 - overlap</code></li> </ol> <p>No raw neighbor sets are ever exchanged. See Protocol Details for the full breakdown.</p>"},{"location":"superpowers/plans/2026-03-17-pplp-python-library/","title":"PPLP Python Library Implementation Plan","text":"<p>For agentic workers: REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (<code>- [ ]</code>) syntax for tracking.</p> <p>Goal: Build a Python library implementing privacy-preserving Common Neighbors link prediction for two-party distributed graphs using OpenMined PSI.</p> <p>Architecture: Single <code>pplp</code> package under <code>lib/</code> with three modules: <code>graph.py</code> (adjacency list wrapper), <code>psi.py</code> (OpenMined PSI cardinality wrapper), <code>protocol.py</code> (CN protocol orchestration). Managed by <code>uv</code>.</p> <p>Tech Stack: Python 3.9+, <code>openmined-psi</code> (ECDH-based PSI), <code>uv</code> for project management, <code>pytest</code> for testing.</p> <p>Spec: <code>docs/superpowers/specs/2026-03-17-pplp-python-library-design.md</code></p>"},{"location":"superpowers/plans/2026-03-17-pplp-python-library/#file-map","title":"File Map","text":"File Purpose <code>pyproject.toml</code> Create: uv project config, dependencies <code>lib/pplp/__init__.py</code> Create: public API exports <code>lib/pplp/graph.py</code> Create: Graph class (adjacency list, neighbor queries) <code>lib/pplp/psi.py</code> Create: <code>psi_cardinality()</code> wrapping openmined-psi <code>lib/pplp/protocol.py</code> Create: <code>compute_cn()</code> protocol orchestration <code>tests/test_graph.py</code> Create: Graph class tests <code>tests/test_psi.py</code> Create: PSI wrapper tests <code>tests/test_protocol.py</code> Create: Protocol tests (paper example + correctness)"},{"location":"superpowers/plans/2026-03-17-pplp-python-library/#task-1-project-scaffolding-with-uv","title":"Task 1: Project scaffolding with uv","text":"<p>Files: - Create: <code>pyproject.toml</code> - Create: <code>lib/pplp/__init__.py</code></p> <ul> <li>[ ] Step 1: Create project files manually</li> </ul> <p>Create <code>lib/pplp/__init__.py</code> (empty) and <code>pyproject.toml</code> directly \u2014 avoids <code>uv init</code> scaffolding artifacts.</p> <pre><code>mkdir -p lib/pplp tests\ntouch lib/pplp/__init__.py\n</code></pre> <ul> <li> <p>[ ] Step 2: Write pyproject.toml <pre><code>[project]\nname = \"pplp\"\nversion = \"0.1.0\"\ndescription = \"Privacy-preserving link prediction library\"\nrequires-python = \"&gt;=3.9\"\ndependencies = [\n    \"openmined-psi&gt;=2.0.0\",\n]\n\n[project.optional-dependencies]\ndev = [\"pytest&gt;=8.0\"]\n\n[build-system]\nrequires = [\"hatchling\"]\nbuild-backend = \"hatchling.build\"\n\n[tool.hatch.build.targets.wheel]\npackages = [\"lib/pplp\"]\n\n[tool.pytest.ini_options]\ntestpaths = [\"tests\"]\n</code></pre></p> </li> <li> <p>[ ] Step 3: Install dependencies</p> </li> </ul> <pre><code>uv sync --all-extras\n</code></pre> <p>Expected: resolves openmined-psi and pytest successfully.</p> <ul> <li>[ ] Step 4: Verify import works</li> </ul> <pre><code>uv run python -c \"import pplp; print('ok')\"\n</code></pre> <p>Expected: prints <code>ok</code>.</p> <ul> <li>[ ] Step 5: Commit</li> </ul> <pre><code>git add pyproject.toml uv.lock lib/pplp/__init__.py\ngit commit -m \"feat: scaffold pplp package with uv\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-17-pplp-python-library/#task-2-graph-class","title":"Task 2: Graph class","text":"<p>Files: - Create: <code>lib/pplp/graph.py</code> - Create: <code>tests/test_graph.py</code></p> <ul> <li>[ ] Step 1: Write failing tests for Graph</li> </ul> <p><code>tests/test_graph.py</code>: <pre><code>from pplp.graph import Graph\n\n\ndef test_add_edge_and_neighbors():\n    g = Graph()\n    g.add_edge(\"A\", \"B\")\n    g.add_edge(\"A\", \"C\")\n    assert g.neighbors(\"A\") == {\"B\", \"C\"}\n    assert g.neighbors(\"B\") == {\"A\"}\n\n\ndef test_neighbors_unknown_node():\n    g = Graph()\n    assert g.neighbors(\"X\") == set()\n\n\ndef test_has_edge():\n    g = Graph()\n    g.add_edge(\"A\", \"B\")\n    assert g.has_edge(\"A\", \"B\") is True\n    assert g.has_edge(\"B\", \"A\") is True\n    assert g.has_edge(\"A\", \"C\") is False\n\n\ndef test_local_intersection():\n    g = Graph()\n    g.add_edge(\"A\", \"C\")\n    g.add_edge(\"A\", \"D\")\n    g.add_edge(\"E\", \"C\")\n    g.add_edge(\"E\", \"F\")\n    assert g.local_intersection(\"A\", \"E\") == {\"C\"}\n\n\ndef test_local_intersection_empty():\n    g = Graph()\n    g.add_edge(\"A\", \"B\")\n    g.add_edge(\"C\", \"D\")\n    assert g.local_intersection(\"A\", \"C\") == set()\n\n\ndef test_from_edge_list():\n    edges = [(\"A\", \"B\"), (\"B\", \"C\")]\n    g = Graph.from_edge_list(edges)\n    assert g.neighbors(\"A\") == {\"B\"}\n    assert g.neighbors(\"B\") == {\"A\", \"C\"}\n</code></pre></p> <ul> <li>[ ] Step 2: Run tests to verify they fail</li> </ul> <pre><code>uv run pytest tests/test_graph.py -v\n</code></pre> <p>Expected: FAIL \u2014 <code>ImportError: cannot import name 'Graph'</code></p> <ul> <li>[ ] Step 3: Implement Graph class</li> </ul> <p><code>lib/pplp/graph.py</code>: <pre><code>from __future__ import annotations\nfrom collections import defaultdict\n\n\nclass Graph:\n    def __init__(self):\n        self._adj: dict[str, set[str]] = defaultdict(set)\n\n    def add_edge(self, u: str, v: str):\n        self._adj[u].add(v)\n        self._adj[v].add(u)\n\n    def neighbors(self, node: str) -&gt; set[str]:\n        return set(self._adj.get(node, set()))\n\n    def has_edge(self, u: str, v: str) -&gt; bool:\n        return v in self._adj.get(u, set())\n\n    def local_intersection(self, x: str, y: str) -&gt; set[str]:\n        return self.neighbors(x) &amp; self.neighbors(y)\n\n    @classmethod\n    def from_edge_list(cls, edges: list[tuple[str, str]]) -&gt; Graph:\n        g = cls()\n        for u, v in edges:\n            g.add_edge(u, v)\n        return g\n</code></pre></p> <ul> <li>[ ] Step 4: Run tests to verify they pass</li> </ul> <pre><code>uv run pytest tests/test_graph.py -v\n</code></pre> <p>Expected: all 6 tests PASS.</p> <ul> <li>[ ] Step 5: Commit</li> </ul> <pre><code>git add lib/pplp/graph.py tests/test_graph.py\ngit commit -m \"feat: add Graph class with adjacency list and neighbor queries\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-17-pplp-python-library/#task-3-psi-cardinality-wrapper","title":"Task 3: PSI cardinality wrapper","text":"<p>Files: - Create: <code>lib/pplp/psi.py</code> - Create: <code>tests/test_psi.py</code></p> <ul> <li>[ ] Step 1: Write failing tests for psi_cardinality</li> </ul> <p><code>tests/test_psi.py</code>: <pre><code>from pplp.psi import psi_cardinality\n\n\ndef test_basic_intersection():\n    a = {\"alice\", \"bob\", \"charlie\"}\n    b = {\"bob\", \"charlie\", \"dave\"}\n    assert psi_cardinality(a, b) == 2\n\n\ndef test_no_intersection():\n    a = {\"alice\", \"bob\"}\n    b = {\"charlie\", \"dave\"}\n    assert psi_cardinality(a, b) == 0\n\n\ndef test_empty_set_a():\n    assert psi_cardinality(set(), {\"a\", \"b\"}) == 0\n\n\ndef test_empty_set_b():\n    assert psi_cardinality({\"a\", \"b\"}, set()) == 0\n\n\ndef test_both_empty():\n    assert psi_cardinality(set(), set()) == 0\n\n\ndef test_identical_sets():\n    s = {\"a\", \"b\", \"c\"}\n    assert psi_cardinality(s, s) == 3\n\n\ndef test_single_element_match():\n    assert psi_cardinality({\"x\"}, {\"x\"}) == 1\n\n\ndef test_single_element_no_match():\n    assert psi_cardinality({\"x\"}, {\"y\"}) == 0\n</code></pre></p> <ul> <li>[ ] Step 2: Run tests to verify they fail</li> </ul> <pre><code>uv run pytest tests/test_psi.py -v\n</code></pre> <p>Expected: FAIL \u2014 <code>ImportError: cannot import name 'psi_cardinality'</code></p> <ul> <li>[ ] Step 3: Implement psi_cardinality</li> </ul> <p><code>lib/pplp/psi.py</code>: <pre><code>import private_set_intersection.python as psi\n\n_FPR = 0.001\n_DATA_STRUCTURE = psi.DataStructure.GCS\n\n\ndef psi_cardinality(set_a: set[str], set_b: set[str]) -&gt; int:\n    if not set_a or not set_b:\n        return 0\n\n    list_a = list(set_a)\n    list_b = list(set_b)\n\n    client = psi.client.CreateWithNewKey(reveal_intersection=False)\n    server = psi.server.CreateWithNewKey(reveal_intersection=False)\n\n    setup = server.CreateSetupMessage(_FPR, len(list_a), list_b, _DATA_STRUCTURE)\n    request = client.CreateRequest(list_a)\n    response = server.ProcessRequest(request)\n\n    return client.GetIntersectionSize(setup, response)\n</code></pre></p> <ul> <li>[ ] Step 4: Run tests to verify they pass</li> </ul> <pre><code>uv run pytest tests/test_psi.py -v\n</code></pre> <p>Expected: all 8 tests PASS.</p> <ul> <li>[ ] Step 5: Commit</li> </ul> <pre><code>git add lib/pplp/psi.py tests/test_psi.py\ngit commit -m \"feat: add PSI cardinality wrapper around openmined-psi\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-17-pplp-python-library/#task-4-protocol-compute_cn","title":"Task 4: Protocol \u2014 compute_cn","text":"<p>Files: - Create: <code>lib/pplp/protocol.py</code> - Create: <code>tests/test_protocol.py</code></p> <p>The test data comes from the presentation's worked example (nodes A and E):</p> <p>Graph A (Alice): - A\u2014B, A\u2014C, A\u2014G, A\u2014H, E\u2014C, E\u2014D</p> <p>Graph B (Bob): - A\u2014C, A\u2014F, A\u2014K, E\u2014B, E\u2014C, E\u2014D, E\u2014F</p> <p>Expected computation: - local1 (\u0393_A(A) \u2229 \u0393_A(E)) = {C}, size 1 - local2 (\u0393_B(A) \u2229 \u0393_B(E)) = {C, F}, size 2 - After removing locals: \u0393_A(A)\\local1 = {H,G,B}, \u0393_A(E)\\local1 = {D}, \u0393_B(A)\\local2 = {K}, \u0393_B(E)\\local2 = {B,D} - crossover1 = |{H,G,B} \u2229 {B,D}| = 1 (B) - crossover2 = |{D} \u2229 {K}| = 0 - overlap = |{C} \u2229 {C,F}| = 1 (C) - CN = 1 + 2 + 1 + 0 - 1 = 3</p> <ul> <li>[ ] Step 1: Write failing tests for compute_cn</li> </ul> <p><code>tests/test_protocol.py</code>: <pre><code>import pytest\nfrom pplp.graph import Graph\nfrom pplp.protocol import compute_cn, DirectLinkFound\n\n\ndef _make_paper_graphs():\n    \"\"\"Graphs from the presentation example (nodes A and E, CN=3).\"\"\"\n    g1 = Graph.from_edge_list([\n        (\"A\", \"B\"), (\"A\", \"C\"), (\"A\", \"G\"), (\"A\", \"H\"),\n        (\"E\", \"C\"), (\"E\", \"D\"),\n    ])\n    g2 = Graph.from_edge_list([\n        (\"A\", \"C\"), (\"A\", \"F\"), (\"A\", \"K\"),\n        (\"E\", \"B\"), (\"E\", \"C\"), (\"E\", \"D\"), (\"E\", \"F\"),\n    ])\n    return g1, g2\n\n\ndef test_paper_example():\n    g1, g2 = _make_paper_graphs()\n    assert compute_cn(g1, g2, \"A\", \"E\") == 3\n\n\ndef test_matches_naive_cn():\n    \"\"\"compute_cn should match a plain set intersection on the merged graph.\"\"\"\n    g1, g2 = _make_paper_graphs()\n\n    merged_neighbors_a = g1.neighbors(\"A\") | g2.neighbors(\"A\")\n    merged_neighbors_e = g1.neighbors(\"E\") | g2.neighbors(\"E\")\n    naive_cn = len(merged_neighbors_a &amp; merged_neighbors_e)\n\n    assert compute_cn(g1, g2, \"A\", \"E\") == naive_cn\n\n\ndef test_no_common_neighbors():\n    g1 = Graph.from_edge_list([(\"X\", \"A\")])\n    g2 = Graph.from_edge_list([(\"Y\", \"B\")])\n    assert compute_cn(g1, g2, \"X\", \"Y\") == 0\n\n\ndef test_node_missing_from_one_graph():\n    g1 = Graph.from_edge_list([(\"A\", \"C\"), (\"B\", \"C\")])\n    g2 = Graph.from_edge_list([(\"A\", \"D\")])\n    # B not in g2, so g2 contributes nothing for B's neighbors\n    assert compute_cn(g1, g2, \"A\", \"B\") == 1  # C is the only CN\n\n\ndef test_both_nodes_missing_from_graph2():\n    g1 = Graph.from_edge_list([(\"A\", \"C\"), (\"B\", \"C\")])\n    g2 = Graph.from_edge_list([(\"X\", \"Y\")])\n    assert compute_cn(g1, g2, \"A\", \"B\") == 1\n\n\ndef test_direct_neighbors_graph1():\n    g1 = Graph.from_edge_list([(\"A\", \"B\")])\n    g2 = Graph.from_edge_list([(\"A\", \"C\"), (\"B\", \"C\")])\n    with pytest.raises(ValueError, match=\"direct neighbors in graph1\"):\n        compute_cn(g1, g2, \"A\", \"B\")\n\n\ndef test_direct_neighbors_graph2():\n    g1 = Graph.from_edge_list([(\"A\", \"C\"), (\"B\", \"C\")])\n    g2 = Graph.from_edge_list([(\"A\", \"B\")])\n    with pytest.raises(DirectLinkFound):\n        compute_cn(g1, g2, \"A\", \"B\")\n</code></pre></p> <ul> <li>[ ] Step 2: Run tests to verify they fail</li> </ul> <pre><code>uv run pytest tests/test_protocol.py -v\n</code></pre> <p>Expected: FAIL \u2014 <code>ImportError: cannot import name 'compute_cn'</code></p> <ul> <li>[ ] Step 3: Implement compute_cn</li> </ul> <p><code>lib/pplp/protocol.py</code>: <pre><code>from pplp.graph import Graph\nfrom pplp.psi import psi_cardinality\n\n\nclass DirectLinkFound(Exception):\n    \"\"\"Raised when x and y are direct neighbors in graph2.\n\n    Per the paper (Section 2.2, assumption 4), Graph 2 halts and informs\n    Graph 1 that a direct link exists, which is stronger evidence than CN.\n    \"\"\"\n\n\ndef compute_cn(graph1: Graph, graph2: Graph, x: str, y: str) -&gt; int:\n    if graph1.has_edge(x, y):\n        raise ValueError(\n            f\"{x} and {y} are direct neighbors in graph1; \"\n            \"no need for the computation\"\n        )\n    if graph2.has_edge(x, y):\n        raise DirectLinkFound(\n            f\"{x} and {y} are direct neighbors in graph2\"\n        )\n\n    local1_set = graph1.local_intersection(x, y)\n    local2_set = graph2.local_intersection(x, y)\n    local1 = len(local1_set)\n    local2 = len(local2_set)\n\n    g1_x = graph1.neighbors(x) - local1_set\n    g1_y = graph1.neighbors(y) - local1_set\n    g2_x = graph2.neighbors(x) - local2_set\n    g2_y = graph2.neighbors(y) - local2_set\n\n    crossover1 = psi_cardinality(g1_x, g2_y)\n    crossover2 = psi_cardinality(g1_y, g2_x)\n    overlap = psi_cardinality(local1_set, local2_set)\n\n    return local1 + local2 + crossover1 + crossover2 - overlap\n</code></pre></p> <ul> <li>[ ] Step 4: Run tests to verify they pass</li> </ul> <pre><code>uv run pytest tests/test_protocol.py -v\n</code></pre> <p>Expected: all 7 tests PASS.</p> <ul> <li>[ ] Step 5: Commit</li> </ul> <pre><code>git add lib/pplp/protocol.py tests/test_protocol.py\ngit commit -m \"feat: implement compute_cn protocol with PSI cardinality calls\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-17-pplp-python-library/#task-5-public-api-exports-and-final-verification","title":"Task 5: Public API exports and final verification","text":"<p>Files: - Modify: <code>lib/pplp/__init__.py</code></p> <ul> <li>[ ] Step 1: Update init.py with exports</li> </ul> <p><code>lib/pplp/__init__.py</code>: <pre><code>from pplp.graph import Graph\nfrom pplp.protocol import compute_cn, DirectLinkFound\n\n__all__ = [\"Graph\", \"compute_cn\", \"DirectLinkFound\"]\n</code></pre></p> <ul> <li>[ ] Step 2: Run full test suite</li> </ul> <pre><code>uv run pytest tests/ -v\n</code></pre> <p>Expected: all tests PASS (21 total across 3 files).</p> <ul> <li>[ ] Step 3: Verify public API import</li> </ul> <pre><code>uv run python -c \"from pplp import Graph, compute_cn; print('API ok')\"\n</code></pre> <p>Expected: prints <code>API ok</code>.</p> <ul> <li>[ ] Step 4: Commit</li> </ul> <pre><code>git add lib/pplp/__init__.py\ngit commit -m \"feat: export public API from pplp package\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-24-distributed-psi-http/","title":"Distributed PSI over HTTP Implementation Plan","text":"<p>For agentic workers: REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (<code>- [ ]</code>) syntax for tracking.</p> <p>Goal: Split <code>compute_cn</code> across two real network services so Party 1 and Party 2 can run on separate machines, with each party holding only their own graph.</p> <p>Architecture: Party 2 runs a FastAPI server that exposes endpoints for the PSI protocol handshake \u2014 it holds Graph 2 in memory, computes its neighbor sets, and responds to PSI setup/respond round-trips. Party 1 holds Graph 1, orchestrates the 3 PSI calls over HTTP via <code>compute_cn_remote</code>, and is the only party that learns the final CN score. The existing local <code>compute_cn(graph1, graph2, x, y)</code> is kept unchanged for tests and demos. Graph and session store are held on <code>app.state</code> (not module globals) so multiple app instances can coexist in the same process during tests.</p> <p>Tech Stack: FastAPI, Uvicorn, HTTPX (sync HTTP client), openmined-psi (protobuf messages serialized as base64 over JSON), <code>private_set_intersection.proto.psi_pb2</code> for deserialization.</p>"},{"location":"superpowers/plans/2026-03-24-distributed-psi-http/#serialization-contract","title":"Serialization Contract","text":"<p>Each PSI call requires two HTTP round-trips. Protobuf objects are serialized as base64-encoded strings in JSON bodies:</p> <pre><code>setup_b64   = base64.b64encode(setup.SerializeToString()).decode()\nsetup_proto = ServerSetup.FromString(base64.b64decode(setup_b64))\n</code></pre> <p>Same pattern for <code>Request</code> and <code>Response</code>.</p>"},{"location":"superpowers/plans/2026-03-24-distributed-psi-http/#api-party-2-endpoints","title":"API: Party 2 Endpoints","text":"<pre><code>POST /prepare\n  Body:     { \"x\": str, \"y\": str }\n  Response: { \"direct_link\": bool, \"local2\": int, \"session_id\": str }\n\nPOST /psi/{session_id}/setup\n  Body:     { \"call\": \"crossover1\"|\"crossover2\"|\"overlap\", \"client_set_size\": int }\n  Response: { \"setup\": str }   # base64 ServerSetup protobuf\n\nPOST /psi/{session_id}/respond\n  Body:     { \"call\": \"crossover1\"|\"crossover2\"|\"overlap\", \"request\": str }  # base64 Request\n  Response: { \"response\": str }  # base64 Response protobuf\n</code></pre> <p>For each <code>call</code>: - <code>crossover1</code>: Party 2 uses <code>g2_y - local2_set</code> - <code>crossover2</code>: Party 2 uses <code>g2_x - local2_set</code> - <code>overlap</code>:    Party 2 uses <code>local2_set</code></p> <p>Known limitation: Sessions are held in memory and never garbage-collected for the lifetime of the server process. For a class demo with low query volume this is fine.</p>"},{"location":"superpowers/plans/2026-03-24-distributed-psi-http/#file-map","title":"File Map","text":"File Action Purpose <code>pyproject.toml</code> Modify Add <code>fastapi</code>, <code>uvicorn[standard]</code>, <code>httpx</code> deps <code>lib/pplp/server/__init__.py</code> Create Empty (makes <code>server</code> a package) <code>lib/pplp/server/session.py</code> Create <code>PsiSession</code> dataclass + <code>SessionStore</code> (in-memory dict) <code>lib/pplp/server/routes.py</code> Create <code>/prepare</code>, <code>/psi/{session_id}/setup</code>, <code>/psi/{session_id}/respond</code> \u2014 accesses graph/store via <code>request.app.state</code> <code>lib/pplp/server/app.py</code> Create <code>create_app(graph)</code> factory (sets <code>app.state</code>), <code>load_graph_from_csv</code>, <code>main()</code> CLI <code>lib/pplp/psi_client.py</code> Create <code>remote_psi_cardinality(http_client, client_set, session_id, call) -&gt; int</code> <code>lib/pplp/client.py</code> Create <code>party2_client(base_url)</code> context manager returning <code>httpx.Client</code> <code>lib/pplp/protocol.py</code> Modify Add <code>compute_cn_remote(graph1, party2_client, x, y) -&gt; int</code> <code>lib/pplp/__init__.py</code> Modify Export <code>compute_cn_remote</code>, <code>party2_client</code> <code>tests/test_server.py</code> Create FastAPI TestClient tests for all 3 endpoints + graph loading <code>tests/test_psi_client.py</code> Create Tests for <code>remote_psi_cardinality</code> using a live TestClient server <code>tests/test_protocol_remote.py</code> Create End-to-end <code>compute_cn_remote</code> tests using TestClient"},{"location":"superpowers/plans/2026-03-24-distributed-psi-http/#task-1-add-dependencies","title":"Task 1: Add Dependencies","text":"<p>Files: - Modify: <code>pyproject.toml</code></p> <ul> <li>[ ] Step 1: Add deps to pyproject.toml</li> </ul> <pre><code>dependencies = [\n    \"openmined-psi&gt;=2.0.0\",\n    \"fastapi&gt;=0.110.0\",\n    \"uvicorn[standard]&gt;=0.29.0\",\n    \"httpx&gt;=0.27.0\",\n]\n</code></pre> <ul> <li>[ ] Step 2: Sync</li> </ul> <pre><code>uv sync --all-extras\n</code></pre> <p>Expected: resolves without conflict.</p> <ul> <li>[ ] Step 3: Verify imports</li> </ul> <pre><code>uv run python -c \"import fastapi; import uvicorn; import httpx; print('ok')\"\n</code></pre> <p>Expected: <code>ok</code></p> <ul> <li>[ ] Step 4: Commit</li> </ul> <pre><code>git add pyproject.toml uv.lock\ngit commit -m \"feat: add fastapi, uvicorn, httpx dependencies\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-24-distributed-psi-http/#task-2-session-store","title":"Task 2: Session Store","text":"<p>Files: - Create: <code>lib/pplp/server/__init__.py</code> - Create: <code>lib/pplp/server/session.py</code></p> <p>The session store holds Party 2's PSI server objects between the setup and respond round-trips. Each session tracks the three possible PSI server objects (one per call type) and the neighbor sets Party 2 computed for this (x, y) query.</p> <ul> <li>[ ] Step 1: Write the failing test</li> </ul> <p>Create <code>tests/test_server.py</code>:</p> <pre><code>import pytest\nfrom pplp.server.session import PsiSession, SessionStore\n\n\ndef test_store_creates_and_retrieves_session():\n    store = SessionStore()\n    session = PsiSession(\n        sets={\"crossover1\": {\"a\", \"b\"}, \"crossover2\": {\"c\"}, \"overlap\": {\"d\"}},\n        local2=2,\n    )\n    sid = store.put(session)\n    assert store.get(sid) is session\n\n\ndef test_store_returns_none_for_unknown_id():\n    store = SessionStore()\n    assert store.get(\"nonexistent\") is None\n\n\ndef test_store_delete_removes_session():\n    store = SessionStore()\n    sid = store.put(PsiSession(sets={}, local2=0))\n    store.delete(sid)\n    assert store.get(sid) is None\n</code></pre> <ul> <li>[ ] Step 2: Run to verify it fails</li> </ul> <pre><code>uv run pytest tests/test_server.py::test_store_creates_and_retrieves_session -v\n</code></pre> <p>Expected: <code>ModuleNotFoundError: No module named 'pplp.server'</code></p> <ul> <li> <p>[ ] Step 3: Create <code>lib/pplp/server/__init__.py</code> (empty file)</p> </li> <li> <p>[ ] Step 4: Create <code>lib/pplp/server/session.py</code></p> </li> </ul> <pre><code>from __future__ import annotations\nimport uuid\nfrom dataclasses import dataclass, field\nfrom typing import Any\n\n\n@dataclass\nclass PsiSession:\n    sets: dict[str, set[str]]     # call -&gt; Party 2's neighbor set for that call\n    local2: int                    # |\u0393\u2082(x) \u2229 \u0393\u2082(y)| \u2014 revealed to Party 1 per protocol\n    servers: dict[str, Any] = field(default_factory=dict)  # call -&gt; psi.server object\n\n\nclass SessionStore:\n    def __init__(self):\n        self._store: dict[str, PsiSession] = {}\n\n    def put(self, session: PsiSession) -&gt; str:\n        sid = str(uuid.uuid4())\n        self._store[sid] = session\n        return sid\n\n    def get(self, session_id: str) -&gt; PsiSession | None:\n        return self._store.get(session_id)\n\n    def delete(self, session_id: str) -&gt; None:\n        self._store.pop(session_id, None)\n</code></pre> <ul> <li>[ ] Step 5: Run tests to verify they pass</li> </ul> <pre><code>uv run pytest tests/test_server.py -v\n</code></pre> <p>Expected: 3 passed.</p> <ul> <li>[ ] Step 6: Commit</li> </ul> <pre><code>git add lib/pplp/server/__init__.py lib/pplp/server/session.py tests/test_server.py\ngit commit -m \"feat: add PsiSession and SessionStore for Party 2 state\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-24-distributed-psi-http/#task-3-party-2-prepare-endpoint","title":"Task 3: Party 2 <code>/prepare</code> Endpoint","text":"<p>Files: - Create: <code>lib/pplp/server/routes.py</code> - Create: <code>lib/pplp/server/app.py</code> - Modify: <code>tests/test_server.py</code></p> <p><code>/prepare</code> is the first call Party 1 makes. Party 2 checks whether x-y are direct neighbors in its graph, computes local2, computes the three neighbor sets it will need for PSI, stores them in a session, and returns the session ID plus local2.</p> <p>Graph and session store are accessed via <code>request.app.state</code> \u2014 not module-level globals. This is essential so multiple <code>create_app()</code> calls in the same test process don't clobber each other.</p> <ul> <li>[ ] Step 1: Write the failing test</li> </ul> <p>Append to <code>tests/test_server.py</code>:</p> <pre><code>from fastapi.testclient import TestClient\nfrom pplp.graph import Graph\nfrom pplp.server.app import create_app\n\n\ndef _paper_graph2():\n    return Graph.from_edge_list([\n        (\"A\", \"C\"), (\"A\", \"F\"), (\"A\", \"K\"),\n        (\"E\", \"B\"), (\"E\", \"C\"), (\"E\", \"D\"), (\"E\", \"F\"),\n    ])\n\n\ndef test_prepare_returns_session_and_local2():\n    app = create_app(_paper_graph2())\n    client = TestClient(app)\n    resp = client.post(\"/prepare\", json={\"x\": \"A\", \"y\": \"E\"})\n    assert resp.status_code == 200\n    data = resp.json()\n    assert data[\"direct_link\"] is False\n    assert data[\"local2\"] == 2          # {C, F}\n    assert \"session_id\" in data\n\n\ndef test_prepare_detects_direct_link():\n    g2 = Graph.from_edge_list([(\"A\", \"E\")])\n    app = create_app(g2)\n    client = TestClient(app)\n    resp = client.post(\"/prepare\", json={\"x\": \"A\", \"y\": \"E\"})\n    assert resp.status_code == 200\n    assert resp.json()[\"direct_link\"] is True\n</code></pre> <ul> <li>[ ] Step 2: Run to verify it fails</li> </ul> <pre><code>uv run pytest tests/test_server.py::test_prepare_returns_session_and_local2 -v\n</code></pre> <p>Expected: <code>ImportError</code> (routes/app don't exist yet).</p> <ul> <li>[ ] Step 3: Create <code>lib/pplp/server/app.py</code></li> </ul> <pre><code>from __future__ import annotations\n\nfrom fastapi import FastAPI\n\nfrom pplp.graph import Graph\nfrom pplp.server.session import SessionStore\n\n\ndef create_app(graph: Graph) -&gt; FastAPI:\n    app = FastAPI(title=\"PPLP Party 2 Server\")\n    app.state.graph = graph\n    app.state.store = SessionStore()\n\n    from pplp.server.routes import router\n    app.include_router(router)\n\n    return app\n</code></pre> <ul> <li>[ ] Step 4: Create <code>lib/pplp/server/routes.py</code></li> </ul> <pre><code>from __future__ import annotations\nimport base64\nfrom typing import Literal\n\nimport private_set_intersection.python as psi\nfrom fastapi import APIRouter, HTTPException, Request\nfrom pydantic import BaseModel\n\nfrom pplp.server.session import PsiSession\n\n_FPR = 0.001\n_DATA_STRUCTURE = psi.DataStructure.GCS\nCallType = Literal[\"crossover1\", \"crossover2\", \"overlap\"]\n\nrouter = APIRouter()\n\n\n# --- /prepare ---\n\nclass PrepareRequest(BaseModel):\n    x: str\n    y: str\n\n\nclass PrepareResponse(BaseModel):\n    direct_link: bool\n    local2: int\n    session_id: str\n\n\n@router.post(\"/prepare\", response_model=PrepareResponse)\ndef prepare(req: PrepareRequest, request: Request):\n    g2 = request.app.state.graph\n    store = request.app.state.store\n\n    x, y = req.x, req.y\n    if g2.has_edge(x, y):\n        return PrepareResponse(direct_link=True, local2=0, session_id=\"\")\n\n    local2_set = g2.local_intersection(x, y)\n    g2_x = g2.neighbors(x) - local2_set\n    g2_y = g2.neighbors(y) - local2_set\n\n    session = PsiSession(\n        sets={\n            \"crossover1\": g2_y,       # Party 2's set for crossover1 = g2_y (excl. local2)\n            \"crossover2\": g2_x,       # Party 2's set for crossover2 = g2_x (excl. local2)\n            \"overlap\":    local2_set, # Party 2's set for overlap\n        },\n        local2=len(local2_set),\n    )\n    sid = store.put(session)\n    return PrepareResponse(direct_link=False, local2=session.local2, session_id=sid)\n</code></pre> <ul> <li>[ ] Step 5: Run tests</li> </ul> <pre><code>uv run pytest tests/test_server.py -v\n</code></pre> <p>Expected: all pass including the 2 new prepare tests.</p> <ul> <li>[ ] Step 6: Commit</li> </ul> <pre><code>git add lib/pplp/server/routes.py lib/pplp/server/app.py tests/test_server.py\ngit commit -m \"feat: add Party 2 /prepare endpoint\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-24-distributed-psi-http/#task-4-party-2-psi-endpoints-setup-and-respond","title":"Task 4: Party 2 PSI Endpoints (<code>/setup</code> and <code>/respond</code>)","text":"<p>Files: - Modify: <code>lib/pplp/server/routes.py</code> - Modify: <code>tests/test_server.py</code></p> <p>These two endpoints implement the two halves of each PSI round-trip. <code>/setup</code> creates a PSI server object for a given call type, generates the setup message, and stores the server in the session. <code>/respond</code> looks up the stored server and processes Party 1's request.</p> <p><code>call</code> is validated via <code>Literal[\"crossover1\", \"crossover2\", \"overlap\"]</code> in the Pydantic model \u2014 FastAPI returns HTTP 422 automatically on invalid values.</p> <ul> <li>[ ] Step 1: Write the failing tests</li> </ul> <p>Append to <code>tests/test_server.py</code>:</p> <pre><code>import base64\nimport private_set_intersection.python as psi\nfrom private_set_intersection.proto.psi_pb2 import ServerSetup, Response\n\n\ndef _get_session(client, x=\"A\", y=\"E\"):\n    resp = client.post(\"/prepare\", json={\"x\": x, \"y\": y})\n    return resp.json()[\"session_id\"]\n\n\ndef test_psi_setup_returns_base64_setup():\n    app = create_app(_paper_graph2())\n    client = TestClient(app)\n    sid = _get_session(client)\n\n    resp = client.post(f\"/psi/{sid}/setup\", json={\"call\": \"crossover1\", \"client_set_size\": 3})\n    assert resp.status_code == 200\n    setup = ServerSetup.FromString(base64.b64decode(resp.json()[\"setup\"]))\n    # GCS structure was requested \u2014 verify it was used\n    assert setup.HasField(\"gcs\")\n\n\ndef test_psi_respond_returns_correct_intersection_count():\n    # crossover1: Party 2 has g2_y = {B, D} (E's neighbors excl. local2={C,F})\n    # Party 1 sends g1_x = {H, G, B} (A's neighbors in g1 excl. local1={C})\n    # Intersection = {B} \u2192 count == 1\n    app = create_app(_paper_graph2())\n    client = TestClient(app)\n    sid = _get_session(client)\n\n    psi_client = psi.client.CreateWithNewKey(reveal_intersection=False)\n    setup_resp = client.post(f\"/psi/{sid}/setup\", json={\"call\": \"crossover1\", \"client_set_size\": 3})\n    setup = ServerSetup.FromString(base64.b64decode(setup_resp.json()[\"setup\"]))\n\n    request = psi_client.CreateRequest([\"H\", \"G\", \"B\"])\n    request_b64 = base64.b64encode(request.SerializeToString()).decode()\n    respond_resp = client.post(f\"/psi/{sid}/respond\", json={\"call\": \"crossover1\", \"request\": request_b64})\n    assert respond_resp.status_code == 200\n\n    response = Response.FromString(base64.b64decode(respond_resp.json()[\"response\"]))\n    count = psi_client.GetIntersectionSize(setup, response)\n    assert count == 1  # only B is shared\n\n\ndef test_psi_setup_unknown_session_returns_404():\n    app = create_app(_paper_graph2())\n    client = TestClient(app)\n    resp = client.post(\"/psi/bad-id/setup\", json={\"call\": \"crossover1\", \"client_set_size\": 1})\n    assert resp.status_code == 404\n\n\ndef test_psi_setup_invalid_call_returns_422():\n    app = create_app(_paper_graph2())\n    client = TestClient(app)\n    sid = _get_session(client)\n    resp = client.post(f\"/psi/{sid}/setup\", json={\"call\": \"invalid_call\", \"client_set_size\": 1})\n    assert resp.status_code == 422\n\n\ndef test_psi_respond_without_setup_returns_400():\n    app = create_app(_paper_graph2())\n    client = TestClient(app)\n    sid = _get_session(client)\n    # No setup was called first \u2014 server object won't exist in session\n    request_b64 = base64.b64encode(b\"fake\").decode()\n    resp = client.post(f\"/psi/{sid}/respond\", json={\"call\": \"crossover1\", \"request\": request_b64})\n    assert resp.status_code == 400\n</code></pre> <ul> <li>[ ] Step 2: Run to verify they fail</li> </ul> <pre><code>uv run pytest tests/test_server.py::test_psi_setup_returns_base64_setup -v\n</code></pre> <p>Expected: 404 (endpoint doesn't exist yet).</p> <ul> <li>[ ] Step 3: Append PSI endpoints to <code>routes.py</code></li> </ul> <pre><code>from private_set_intersection.proto.psi_pb2 import Request as PsiRequest\n\n\nclass PsiSetupRequest(BaseModel):\n    call: CallType\n    client_set_size: int\n\n\nclass PsiSetupResponse(BaseModel):\n    setup: str      # base64 ServerSetup\n\n\nclass PsiRespondRequest(BaseModel):\n    call: CallType\n    request: str    # base64 Request\n\n\nclass PsiRespondResponse(BaseModel):\n    response: str   # base64 Response\n\n\n@router.post(\"/psi/{session_id}/setup\", response_model=PsiSetupResponse)\ndef psi_setup(session_id: str, req: PsiSetupRequest, request: Request):\n    store = request.app.state.store\n    session = store.get(session_id)\n    if session is None:\n        raise HTTPException(status_code=404, detail=\"Session not found\")\n\n    party2_set = list(session.sets[req.call])\n    server = psi.server.CreateWithNewKey(reveal_intersection=False)\n    setup = server.CreateSetupMessage(_FPR, req.client_set_size, party2_set, _DATA_STRUCTURE)\n    session.servers[req.call] = server\n\n    return PsiSetupResponse(setup=base64.b64encode(setup.SerializeToString()).decode())\n\n\n@router.post(\"/psi/{session_id}/respond\", response_model=PsiRespondResponse)\ndef psi_respond(session_id: str, req: PsiRespondRequest, request: Request):\n    store = request.app.state.store\n    session = store.get(session_id)\n    if session is None:\n        raise HTTPException(status_code=404, detail=\"Session not found\")\n\n    server = session.servers.get(req.call)\n    if server is None:\n        raise HTTPException(status_code=400, detail=f\"Setup not called for call '{req.call}'\")\n\n    request_proto = PsiRequest.FromString(base64.b64decode(req.request))\n    response = server.ProcessRequest(request_proto)\n    return PsiRespondResponse(response=base64.b64encode(response.SerializeToString()).decode())\n</code></pre> <ul> <li>[ ] Step 4: Run all server tests</li> </ul> <pre><code>uv run pytest tests/test_server.py -v\n</code></pre> <p>Expected: all pass.</p> <ul> <li>[ ] Step 5: Commit</li> </ul> <pre><code>git add lib/pplp/server/routes.py tests/test_server.py\ngit commit -m \"feat: add Party 2 PSI setup and respond endpoints\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-24-distributed-psi-http/#task-5-party-1-psi_clientpy","title":"Task 5: Party 1 <code>psi_client.py</code>","text":"<p>Files: - Create: <code>lib/pplp/psi_client.py</code> - Create: <code>tests/test_psi_client.py</code></p> <p><code>remote_psi_cardinality</code> performs one full PSI call over HTTP against Party 2's server. It is the HTTP analogue of <code>psi_cardinality</code> in <code>psi.py</code>. It accepts any object with a <code>.post()</code> method (both <code>httpx.Client</code> and <code>TestClient</code> qualify).</p> <ul> <li>[ ] Step 1: Write the failing test</li> </ul> <p>Create <code>tests/test_psi_client.py</code>:</p> <pre><code>import pytest\nfrom fastapi.testclient import TestClient\n\nfrom pplp.graph import Graph\nfrom pplp.psi_client import remote_psi_cardinality\nfrom pplp.server.app import create_app\n\n\ndef _g2():\n    return Graph.from_edge_list([(\"A\", \"C\"), (\"A\", \"F\"), (\"A\", \"K\"),\n                                  (\"E\", \"B\"), (\"E\", \"C\"), (\"E\", \"D\"), (\"E\", \"F\")])\n\n\ndef _session(client, x=\"A\", y=\"E\"):\n    return client.post(\"/prepare\", json={\"x\": x, \"y\": y}).json()[\"session_id\"]\n\n\ndef test_remote_psi_cardinality_correct_count():\n    # crossover1: Party 2 has g2_y = {B, D}; Party 1 sends {H, G, B} \u2192 intersection = {B} = 1\n    app = create_app(_g2())\n    http_client = TestClient(app)\n    sid = _session(http_client)\n    assert remote_psi_cardinality(http_client, {\"H\", \"G\", \"B\"}, sid, \"crossover1\") == 1\n\n\ndef test_remote_psi_cardinality_no_intersection():\n    # Z, W are not in any of g2's sets\n    app = create_app(_g2())\n    http_client = TestClient(app)\n    sid = _session(http_client)\n    assert remote_psi_cardinality(http_client, {\"Z\", \"W\"}, sid, \"crossover1\") == 0\n\n\ndef test_remote_psi_cardinality_empty_client_set_returns_zero_without_http():\n    # Empty set short-circuits before making any HTTP call\n    app = create_app(_g2())\n    http_client = TestClient(app)\n    sid = _session(http_client)\n    assert remote_psi_cardinality(http_client, set(), sid, \"crossover1\") == 0\n</code></pre> <ul> <li>[ ] Step 2: Run to verify it fails</li> </ul> <pre><code>uv run pytest tests/test_psi_client.py::test_remote_psi_cardinality_correct_count -v\n</code></pre> <p>Expected: <code>ModuleNotFoundError: No module named 'pplp.psi_client'</code></p> <ul> <li>[ ] Step 3: Create <code>lib/pplp/psi_client.py</code></li> </ul> <pre><code>from __future__ import annotations\nimport base64\n\nimport private_set_intersection.python as psi\nfrom private_set_intersection.proto.psi_pb2 import ServerSetup, Response as PsiResponse\n\n_FPR = 0.001\n\n\ndef remote_psi_cardinality(\n    http_client,\n    client_set: set[str],\n    session_id: str,\n    call: str,\n) -&gt; int:\n    \"\"\"Perform one PSI-cardinality call against Party 2's server.\n\n    Args:\n        http_client: An httpx.Client pointed at Party 2's base URL,\n                     or a FastAPI TestClient (same interface).\n        client_set:  Party 1's set for this PSI call.\n        session_id:  Session ID returned by POST /prepare.\n        call:        One of \"crossover1\", \"crossover2\", \"overlap\".\n    \"\"\"\n    if not client_set:\n        return 0\n\n    psi_client = psi.client.CreateWithNewKey(reveal_intersection=False)\n    client_list = list(client_set)\n\n    setup_resp = http_client.post(\n        f\"/psi/{session_id}/setup\",\n        json={\"call\": call, \"client_set_size\": len(client_list)},\n    )\n    setup_resp.raise_for_status()\n    setup = ServerSetup.FromString(base64.b64decode(setup_resp.json()[\"setup\"]))\n\n    request = psi_client.CreateRequest(client_list)\n    request_b64 = base64.b64encode(request.SerializeToString()).decode()\n    respond_resp = http_client.post(\n        f\"/psi/{session_id}/respond\",\n        json={\"call\": call, \"request\": request_b64},\n    )\n    respond_resp.raise_for_status()\n    response = PsiResponse.FromString(base64.b64decode(respond_resp.json()[\"response\"]))\n\n    return psi_client.GetIntersectionSize(setup, response)\n</code></pre> <ul> <li>[ ] Step 4: Run tests</li> </ul> <pre><code>uv run pytest tests/test_psi_client.py -v\n</code></pre> <p>Expected: all pass.</p> <ul> <li>[ ] Step 5: Commit</li> </ul> <pre><code>git add lib/pplp/psi_client.py tests/test_psi_client.py\ngit commit -m \"feat: add remote_psi_cardinality HTTP client\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-24-distributed-psi-http/#task-6-compute_cn_remote-in-protocolpy","title":"Task 6: <code>compute_cn_remote</code> in <code>protocol.py</code>","text":"<p>Files: - Modify: <code>lib/pplp/protocol.py</code> - Create: <code>tests/test_protocol_remote.py</code></p> <p><code>compute_cn_remote</code> is the distributed analogue of <code>compute_cn</code>. Party 1 holds only <code>graph1</code> and an HTTP client pointing at Party 2's server.</p> <ul> <li>[ ] Step 1: Write the failing test</li> </ul> <p>Create <code>tests/test_protocol_remote.py</code>:</p> <pre><code>import pytest\nfrom fastapi.testclient import TestClient\n\nfrom pplp.graph import Graph\nfrom pplp.protocol import compute_cn_remote, DirectLinkFound\nfrom pplp.server.app import create_app\n\n\ndef _graphs():\n    g1 = Graph.from_edge_list([\n        (\"A\", \"B\"), (\"A\", \"C\"), (\"A\", \"G\"), (\"A\", \"H\"),\n        (\"E\", \"C\"), (\"E\", \"D\"),\n    ])\n    g2 = Graph.from_edge_list([\n        (\"A\", \"C\"), (\"A\", \"F\"), (\"A\", \"K\"),\n        (\"E\", \"B\"), (\"E\", \"C\"), (\"E\", \"D\"), (\"E\", \"F\"),\n    ])\n    return g1, g2\n\n\ndef test_paper_example_remote():\n    g1, g2 = _graphs()\n    party2 = TestClient(create_app(g2))\n    assert compute_cn_remote(g1, party2, \"A\", \"E\") == 3\n\n\ndef test_remote_matches_local():\n    from pplp.protocol import compute_cn\n    g1, g2 = _graphs()\n    party2 = TestClient(create_app(g2))\n    assert compute_cn_remote(g1, party2, \"A\", \"E\") == compute_cn(g1, g2, \"A\", \"E\")\n\n\ndef test_remote_direct_link_graph1_raises():\n    g1 = Graph.from_edge_list([(\"A\", \"B\")])\n    g2 = Graph.from_edge_list([(\"A\", \"C\"), (\"B\", \"C\")])\n    party2 = TestClient(create_app(g2))\n    with pytest.raises(ValueError, match=\"direct neighbors in graph1\"):\n        compute_cn_remote(g1, party2, \"A\", \"B\")\n\n\ndef test_remote_direct_link_graph2_raises():\n    g1 = Graph.from_edge_list([(\"A\", \"C\"), (\"B\", \"C\")])\n    g2 = Graph.from_edge_list([(\"A\", \"B\")])\n    party2 = TestClient(create_app(g2))\n    with pytest.raises(DirectLinkFound):\n        compute_cn_remote(g1, party2, \"A\", \"B\")\n\n\ndef test_remote_no_common_neighbors():\n    # Non-empty but fully disjoint neighbor sets \u2014 PSI calls actually fire\n    g1 = Graph.from_edge_list([(\"X\", \"P\"), (\"Y\", \"Q\")])\n    g2 = Graph.from_edge_list([(\"X\", \"R\"), (\"Y\", \"S\")])\n    party2 = TestClient(create_app(g2))\n    assert compute_cn_remote(g1, party2, \"X\", \"Y\") == 0\n</code></pre> <ul> <li>[ ] Step 2: Run to verify it fails</li> </ul> <pre><code>uv run pytest tests/test_protocol_remote.py::test_paper_example_remote -v\n</code></pre> <p>Expected: <code>ImportError</code> (<code>compute_cn_remote</code> doesn't exist).</p> <ul> <li>[ ] Step 3: Add <code>compute_cn_remote</code> to <code>protocol.py</code></li> </ul> <pre><code>def compute_cn_remote(graph1: Graph, party2_client, x: str, y: str) -&gt; int:\n    \"\"\"Distributed compute_cn \u2014 Party 1 holds graph1, Party 2 is a remote HTTP service.\n\n    Args:\n        graph1:        Party 1's graph.\n        party2_client: An httpx.Client or FastAPI TestClient pointed at Party 2's server.\n        x, y:          The candidate node pair (must match node IDs used in both graphs).\n    \"\"\"\n    from pplp.psi_client import remote_psi_cardinality\n\n    if graph1.has_edge(x, y):\n        raise ValueError(\n            f\"{x} and {y} are direct neighbors in graph1; \"\n            \"no need for the computation\"\n        )\n\n    prep_resp = party2_client.post(\"/prepare\", json={\"x\": x, \"y\": y})\n    prep_resp.raise_for_status()\n    prep = prep_resp.json()\n\n    if prep[\"direct_link\"]:\n        raise DirectLinkFound(f\"{x} and {y} are direct neighbors in graph2\")\n\n    local2 = prep[\"local2\"]\n    session_id = prep[\"session_id\"]\n\n    local1_set = graph1.local_intersection(x, y)\n    local1 = len(local1_set)\n\n    g1_x = graph1.neighbors(x) - local1_set\n    g1_y = graph1.neighbors(y) - local1_set\n\n    crossover1 = remote_psi_cardinality(party2_client, g1_x, session_id, \"crossover1\")\n    crossover2 = remote_psi_cardinality(party2_client, g1_y, session_id, \"crossover2\")\n    overlap = remote_psi_cardinality(party2_client, local1_set, session_id, \"overlap\")\n\n    return local1 + local2 + crossover1 + crossover2 - overlap\n</code></pre> <ul> <li>[ ] Step 4: Run all protocol remote tests</li> </ul> <pre><code>uv run pytest tests/test_protocol_remote.py -v\n</code></pre> <p>Expected: all pass.</p> <ul> <li>[ ] Step 5: Run the full test suite to check nothing broke</li> </ul> <pre><code>uv run pytest -v\n</code></pre> <p>Expected: all existing tests plus new tests pass.</p> <ul> <li>[ ] Step 6: Commit</li> </ul> <pre><code>git add lib/pplp/protocol.py tests/test_protocol_remote.py\ngit commit -m \"feat: add compute_cn_remote for distributed two-party protocol\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-24-distributed-psi-http/#task-7-export-cli-entrypoint-graph-loading","title":"Task 7: Export, CLI Entrypoint, Graph Loading","text":"<p>Files: - Modify: <code>lib/pplp/__init__.py</code> - Modify: <code>lib/pplp/server/app.py</code> - Create: <code>lib/pplp/client.py</code> - Modify: <code>pyproject.toml</code></p> <p>Party 2 needs to be runnable as a standalone process on a real machine, loading a graph from a CSV edge-list file.</p> <ul> <li>[ ] Step 1: Add graph loading + CLI to <code>server/app.py</code></li> </ul> <p>Replace the contents of <code>lib/pplp/server/app.py</code> with:</p> <pre><code>from __future__ import annotations\nimport csv\n\nfrom fastapi import FastAPI\n\nfrom pplp.graph import Graph\nfrom pplp.server.session import SessionStore\n\n\ndef create_app(graph: Graph) -&gt; FastAPI:\n    app = FastAPI(title=\"PPLP Party 2 Server\")\n    app.state.graph = graph\n    app.state.store = SessionStore()\n\n    from pplp.server.routes import router\n    app.include_router(router)\n\n    return app\n\n\ndef load_graph_from_csv(path: str) -&gt; Graph:\n    \"\"\"Load a graph from a two-column CSV (no header): one edge per row as 'u,v'.\"\"\"\n    edges = []\n    with open(path, newline=\"\") as f:\n        for row in csv.reader(f):\n            if len(row) &gt;= 2:\n                edges.append((row[0].strip(), row[1].strip()))\n    return Graph.from_edge_list(edges)\n\n\ndef main():\n    \"\"\"CLI entrypoint: pplp-server &lt;graph.csv&gt; [--host HOST] [--port PORT]\"\"\"\n    import argparse\n    import uvicorn\n\n    parser = argparse.ArgumentParser(description=\"Run the PPLP Party 2 server\")\n    parser.add_argument(\"graph_file\", help=\"Path to edge-list CSV (u,v per line, no header)\")\n    parser.add_argument(\"--host\", default=\"0.0.0.0\")\n    parser.add_argument(\"--port\", type=int, default=8000)\n    args = parser.parse_args()\n\n    graph = load_graph_from_csv(args.graph_file)\n    app = create_app(graph)\n    uvicorn.run(app, host=args.host, port=args.port)\n\n\nif __name__ == \"__main__\":\n    main()\n</code></pre> <ul> <li>[ ] Step 2: Write a test for <code>load_graph_from_csv</code></li> </ul> <p>Append to <code>tests/test_server.py</code>:</p> <pre><code>import os\nimport tempfile\nfrom pplp.server.app import load_graph_from_csv\n\n\ndef test_load_graph_from_csv():\n    with tempfile.NamedTemporaryFile(mode=\"w\", suffix=\".csv\", delete=False) as f:\n        f.write(\"A,B\\nB,C\\nA,C\\n\")\n        path = f.name\n    try:\n        g = load_graph_from_csv(path)\n        assert g.has_edge(\"A\", \"B\")\n        assert g.has_edge(\"B\", \"C\")\n        assert g.has_edge(\"A\", \"C\")\n    finally:\n        os.unlink(path)\n</code></pre> <ul> <li>[ ] Step 3: Create <code>lib/pplp/client.py</code></li> </ul> <pre><code>from __future__ import annotations\nfrom contextlib import contextmanager\n\nimport httpx\n\n\n@contextmanager\ndef party2_client(base_url: str, timeout: float = 30.0):\n    \"\"\"Context manager returning an httpx.Client for Party 2's server.\n\n    Usage:\n        with party2_client(\"http://192.168.1.10:8000\") as client:\n            result = compute_cn_remote(graph1, client, \"A\", \"E\")\n    \"\"\"\n    with httpx.Client(base_url=base_url, timeout=timeout) as client:\n        yield client\n</code></pre> <ul> <li>[ ] Step 4: Update <code>lib/pplp/__init__.py</code></li> </ul> <pre><code>from pplp.client import party2_client\nfrom pplp.graph import Graph\nfrom pplp.protocol import compute_cn, compute_cn_remote, DirectLinkFound\n\n__all__ = [\"Graph\", \"compute_cn\", \"compute_cn_remote\", \"DirectLinkFound\", \"party2_client\"]\n</code></pre> <ul> <li>[ ] Step 5: Register CLI script in <code>pyproject.toml</code></li> </ul> <p>Add under <code>[project]</code>:</p> <pre><code>[project.scripts]\npplp-server = \"pplp.server.app:main\"\n</code></pre> <ul> <li>[ ] Step 6: Run full suite</li> </ul> <pre><code>uv run pytest -v\n</code></pre> <p>Expected: all pass.</p> <ul> <li>[ ] Step 7: Verify CLI is installed</li> </ul> <pre><code>uv sync &amp;&amp; uv run pplp-server --help\n</code></pre> <p>Expected: usage message with <code>graph_file</code>, <code>--host</code>, <code>--port</code>.</p> <ul> <li>[ ] Step 8: Commit</li> </ul> <pre><code>git add lib/pplp/__init__.py lib/pplp/server/app.py lib/pplp/client.py pyproject.toml tests/test_server.py\ngit commit -m \"feat: add pplp-server CLI, load_graph_from_csv, party2_client\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-24-distributed-psi-http/#two-machine-demo-usage","title":"Two-Machine Demo Usage","text":"<p>After this plan is complete, here's what the demo looks like:</p> <p>Machine 2 (Party 2) \u2014 terminal: <pre><code>pip install pplp\n# create party2_graph.csv with one edge per line: u,v\npplp-server party2_graph.csv --host 0.0.0.0 --port 8000\n</code></pre></p> <p>Machine 1 (Party 1) \u2014 Python: <pre><code>from pplp import Graph, compute_cn_remote, party2_client\n\ng1 = Graph.from_edge_list([...])  # Party 1's private graph\n\nwith party2_client(\"http://&lt;machine2-ip&gt;:8000\") as client:\n    cn = compute_cn_remote(g1, client, \"Alice\", \"Bob\")\n    print(f\"Common Neighbors: {cn}\")\n</code></pre></p>"},{"location":"superpowers/plans/2026-03-24-distributed-psi-http/#privacy-notes-for-writeup","title":"Privacy Notes (for writeup)","text":"<ul> <li>Party 2 learns: <code>x</code>, <code>y</code>, <code>|client_set|</code> for each PSI call (via <code>client_set_size</code>), and the size of its own local intersection. This is the intentional semi-honest leakage described in Ayday et al.</li> <li>Party 1 learns: <code>local2</code> (explicit from <code>/prepare</code>), and the three PSI intersection sizes. Party 1 never sees Party 2's raw adjacency list.</li> <li><code>client_set_size</code> trust: Under the semi-honest model, Party 1 is trusted to send the correct set size. A dishonest Party 1 could skew the Bloom filter sizing, but this is out-of-scope per the paper's threat model.</li> <li>Session cleanup: Sessions are never deleted from memory. Not a concern for a demo; a production system would add a TTL.</li> </ul>"},{"location":"superpowers/specs/2026-03-17-pplp-python-library-design/","title":"PPLP Python Library \u2014 Design Spec","text":""},{"location":"superpowers/specs/2026-03-17-pplp-python-library-design/#summary","title":"Summary","text":"<p>A Python library implementing privacy-preserving link prediction (Common Neighbors) for two-party distributed graphs, based on Demirag/Ayday et al. 2022. Uses OpenMined PSI for cardinality-only private set intersection. Local-only execution initially; networking layer deferred to a future phase.</p>"},{"location":"superpowers/specs/2026-03-17-pplp-python-library-design/#package-structure","title":"Package Structure","text":"<pre><code>lib/\n    pplp/\n        __init__.py          # public API exports\n        graph.py             # Graph class\n        protocol.py          # compute_cn() orchestration\n        psi.py               # OpenMined PSI wrapper\npyproject.toml               # uv-managed project\n</code></pre>"},{"location":"superpowers/specs/2026-03-17-pplp-python-library-design/#dependencies","title":"Dependencies","text":"<ul> <li><code>openmined-psi</code> \u2014 ECDH-based PSI with cardinality-only mode (C++ backend, pip-installable, macOS ARM + Linux)</li> <li><code>networkx</code> (optional) \u2014 only needed for <code>Graph.from_networkx()</code> convenience method</li> <li>Managed via <code>uv</code> with <code>pyproject.toml</code></li> </ul>"},{"location":"superpowers/specs/2026-03-17-pplp-python-library-design/#components","title":"Components","text":""},{"location":"superpowers/specs/2026-03-17-pplp-python-library-design/#graphpy-graph-class","title":"<code>graph.py</code> \u2014 Graph class","text":"<p>Thin wrapper around an adjacency list (<code>dict[str, set[str]]</code>). Node IDs are strings (parties agree on identifiers per paper assumptions).</p> <p>Methods: - <code>neighbors(node: str) -&gt; set[str]</code> \u2014 return neighbor set - <code>local_intersection(x: str, y: str) -&gt; set[str]</code> \u2014 return <code>Gamma(x) &amp; Gamma(y)</code> - <code>has_edge(x: str, y: str) -&gt; bool</code> - <code>add_edge(u: str, v: str)</code> \u2014 add undirected edge - <code>from_edge_list(edges: list[tuple[str, str]]) -&gt; Graph</code> \u2014 class method - <code>from_networkx(G: nx.Graph) -&gt; Graph</code> \u2014 class method</p>"},{"location":"superpowers/specs/2026-03-17-pplp-python-library-design/#psipy-psi-wrapper","title":"<code>psi.py</code> \u2014 PSI wrapper","text":"<p>Single function isolating the OpenMined PSI dependency:</p> <pre><code>def psi_cardinality(set_a: set[str], set_b: set[str]) -&gt; int\n</code></pre> <p>Runs OpenMined PSI client/server in-process with <code>reveal_intersection=False</code> (cardinality-only). Returns the intersection size. Short-circuits to 0 if either input set is empty.</p> <p>This isolation means swapping PSI backends or adding networking only touches this file.</p>"},{"location":"superpowers/specs/2026-03-17-pplp-python-library-design/#protocolpy-protocol-orchestration","title":"<code>protocol.py</code> \u2014 Protocol orchestration","text":"<p>Single function implementing the Demirag/Ayday et al. protocol. <code>graph1</code> is the client/requester (learns the result), <code>graph2</code> is the server/helper (learns nothing). This asymmetry mirrors the paper's model and matters for the future networking layer.</p> <pre><code>def compute_cn(graph1: Graph, graph2: Graph, x: str, y: str) -&gt; int\n</code></pre> <p>Steps: 1. Validate preconditions:    - If x-y are direct neighbors in graph1, raise/return early (Graph 1 has no need for the computation)    - If x-y are direct neighbors in graph2, return a sentinel or raise indicating a direct link was found (per paper Section 2.2, assumption 4: Graph 2 halts and informs Graph 1)    - If a node is absent from a graph, treat its neighbor set as empty (the protocol still works with zero-cardinality contributions) 2. Compute local intersections:    - <code>local1_set = graph1.local_intersection(x, y)</code>    - <code>local2_set = graph2.local_intersection(x, y)</code>    - <code>local1 = len(local1_set)</code>, <code>local2 = len(local2_set)</code> 3. Compute reduced neighbor sets (remove local intersection members):    - <code>g1_x = graph1.neighbors(x) - local1_set</code>    - <code>g1_y = graph1.neighbors(y) - local1_set</code>    - <code>g2_x = graph2.neighbors(x) - local2_set</code>    - <code>g2_y = graph2.neighbors(y) - local2_set</code> 4. Three PSI-cardinality calls:    - <code>crossover1 = psi_cardinality(g1_x, g2_y)</code>    - <code>crossover2 = psi_cardinality(g1_y, g2_x)</code>    - <code>overlap = psi_cardinality(local1_set, local2_set)</code> 5. Return <code>local1 + local2 + crossover1 + crossover2 - overlap</code></p>"},{"location":"superpowers/specs/2026-03-17-pplp-python-library-design/#__init__py-public-api","title":"<code>__init__.py</code> \u2014 Public API","text":"<p>Exports: <code>Graph</code>, <code>compute_cn</code>, <code>DirectLinkFound</code></p>"},{"location":"superpowers/specs/2026-03-17-pplp-python-library-design/#testing","title":"Testing","text":"<ul> <li>Paper example: Reproduce Figure 6 from Demirag/Ayday et al. (nodes 1 and 6, expected CN=3)</li> <li>Correctness check: Compare <code>compute_cn</code> output against a plain (non-private) common neighbor computation on the merged graph for various test cases</li> <li>Edge cases: Empty neighbor sets, no overlap between graphs, single-node intersections</li> </ul>"},{"location":"superpowers/specs/2026-03-17-pplp-python-library-design/#future-work-not-in-scope","title":"Future Work (not in scope)","text":"<ul> <li>Network layer: socket/gRPC communication so Graph 1 and Graph 2 run on separate machines</li> <li>Additional similarity metrics: Jaccard, Adamic-Adar</li> <li>Streamlit web dashboard</li> <li>Heavier homomorphic variant (zero leakage, per Section 4.1 of the paper)</li> </ul>"}]}